<!DOCTYPE html>
<html>
<head>
  <title>Blogging App</title>
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body id="main">

<div class="navbar navbar-inverse">
  <div class='container-fluid'>
    <div class="navbar-header" id="foo">
      <%= link_to 'Bloggerz Paradise', root_path %>
    </div>
    <ul class="nav navbar-nav">
      <li class="active">About Us</li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if !logged_in? %>
          <li><%= link_to '<span class="glyphicon glyphicon-log-in "></span>&nbspLogin'.html_safe, login_path %></li>
          <li><%= link_to '<span class="glyphicon glyphicon-user "></span>&nbspSign Up'.html_safe, signup_path %></li>
      <% else %>
          <li>
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <span class="glyphicon glyphicon-user "></span>&nbsp<%= @current_user.name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'My Blogs'.html_safe, @current_user %>
              <li><%= link_to '<span class="glyphicon glyphicon-log-out "></span>Logout'.html_safe, logout_path, method: :delete %>
            </ul>

          </li>
      <% end %>
    </ul>
  </div>
</div>
<div class="mainContent">
  <%= yield %>
</div>
<script language="javascript">
  function autoResizeDiv()
  {
    document.getElementById('main').style.height = window.innerHeight +'px';
  }
  window.onresize = autoResizeDiv;
  autoResizeDiv();
</script>
</body>
</html>
